# Workspace design: one terminal, shared instruments

Evidence and State Management retains and governs information and includes the firm's internal workspace for evidence preparation, state inspection, admission and release review. This document records the shared design language and interaction patterns, the surfaces built on them, and the truths the design must preserve. The dated verification receipts describe the version tested at that time, not a guarantee about subsequent changes.

## The design language

Shared tokens live in `src/app/globals.css`. Existing specialist views also contain local sizing; this iteration does not claim to have migrated every page-local style.

- **Type.** A seven-step rem scale, 12–24 px at the default root size, with a 16 px body. Shared controls and section labels use 14 px; metadata uses 12–13 px. Monospace identifies values, digests, timestamps and instrument labels; explanations remain proportional text.
- **Space and surfaces.** Blue-black opaque surfaces, fine evidence frames and corner registration marks. Broad, desaturated cyan section bands and curved outer ends belong to navigation; evidence panels remain square. A faint unscaled grid is decorative, never a coordinate system. Desktop chrome uses an 80 px minimum header and a 232 px rail; below 768 px the header becomes three rows with a 9 rem minimum, and below 1024 px navigation becomes a horizontal strip.
- **Colour.** Existing semantic status and epistemic hues are unchanged. Gold marks selection and primary action. The separate `--chrome-*` tokens group navigation and frame inspectors; they do not assert observation, verification or runtime health. Text has four roles: heading, primary, secondary, muted. Static contrast tests cover all four on the six opaque grounds, semantic label colours, and the selected palette row; these are not a rendered accessibility audit.
- **Registers.** A ledger table is `.register`: a horizontal scroll region that shows which edge has more table past it, in four background layers and no script, so a column beyond the right edge is never a column nobody knows is there. **A register is a table where its columns fit and a list where they do not**, and the condition is asked of the register — a container query on the surface column, not a viewport breakpoint. Stacked, each row is a block, each cell carries its own label as real markup, and the table roles are restated because changing `display` takes the implicit ones away. Every `.surface` scroll region holding a ledger table takes the same mark — the selector says it once rather than twenty-one files saying it each — and a browser test holds six of those pages to carrying it. Identifiers, timestamps and versions do not break mid-token inside one — a release id split across four lines cannot be compared down a column — and a header may take `.th-wrap` where its words are wider than its values. Side padding is 8px: six columns spend twelve gutters.
- **Controls.** `.btn` with `-sm`, `-primary`, `-quiet`; rows that can be selected carry `.row-selectable` with `aria-pressed`, `aria-selected` or `data-selected`; keyboard hints are `.kbd`; first use is `.empty-state`. Product links read their selected state from supported route/query contracts, not a hardcoded Caravan selection. Section headers can wrap their actions instead of squeezing their title.
- **Motion.** `--motion-fast` (120 ms) for controls, `--motion-base` (180 ms, ease-out) for a panel entering, `--motion-slow` (260 ms) for a one-off flash. Nothing animates while the system waits: progress is named in the status line, not spun. `prefers-reduced-motion` disables all of it.
- **Waiting.** A surface waiting for its source shows `SurfaceLoading`: a framed panel, at the page's own margin, naming the source *that surface* reads. Not a skeleton — a skeleton draws rows that are not there, and on these surfaces an empty region is a fact about the corpus, so a grey shape where a row will be is a shape the reader has been told to expect and may not get.

## The shell

`src/components/shell/AppShell.tsx`: a skip link, the sticky top bar (brand, area / page, page search and product links), then the body: one primary navigation and the main surface. Navigation is data (`nav.ts`): Products, Acquisition, Corpus, Notations, Inquiry and Coordination. Numbered section bands and their total are derived from that registry, so another area does not require another navigation implementation. The rail names all three product lines and explains the fixture/local-rail boundary. The home link targets `/`, allowing the root console to accompany these changes without hardwiring another landing page.

The horizontal list owns scrolling on narrow screens; the containing sidebar owns vertical scrolling on desktop. Navigation and arrow-key focus reveal the destination in that actual owner. Product links scope Releases and Retractions only; a product query on Earth or another unscoped instrument does not light a misleading selection.

**The atlas** (`WorkspaceAtlas.tsx`, on the console) is that same registry as a figure, because the rail is a good list and a poor map: it answers where to go next and not what is here. One cell per destination and every cell the same size, so an area's block is exactly as large as the number of places it holds and the figure's ragged edge is the shape of this system rather than a composition; reading order is registry order, so a cell's number is its position in the order `Alt` with an arrow steps through. Band heads are a mono label on a hairline (the band you are in takes the ice light the rail uses for "here"), and carry the area's activity line, which the rail has nowhere to show. Nothing else is encoded: `ATLAS_LOSS` states on the surface that size is a count and not an importance, that adjacency is an order and not a workflow, and that the figure reads no store, no rail and no clock — so no cell reports whether its destination is reachable, holds data or is working. It is one tab stop like the rail, with the arrows moving along a band and between bands. Derived from `nav.ts`, so an eighth area appears in the drawing without a line of drawing code. Full account in [UX architecture](UX_ARCHITECTURE.md) under "Knowing what is there".

## Reference synthesis (2026-09-08)

The founder's eighteen references are design inputs, not datasets, interface specifications or source licences. No supplied image is bundled into the application. The GIF was considered from its supplied still, not reviewed frame by frame.

| Reference family | Applied to this terminal | Boundary |
| --- | --- | --- |
| LCARS grouping and workstation windows | Numbered navigation bands, limited curved chrome, stable named panels | No fictional controls or ornamental serial numbers |
| Cyan HUDs and scientific blueprints | Dark grounds, clear section rules, compact mono metadata and drawn instrument frames | No bloom, background video or animated loading theatre |
| Amber radar and retro scientific terminals | Amber selection and action, strong visual separation from the cyan frame | Existing status vocabulary remains authoritative |
| Globe, orbital and geographic displays | Earth remains the main spatial instrument, with a framed, wrapping readout and adjacent inspector | No new layer, coordinate or live-feed claim |
| Air-traffic, LiDAR and graph views | Keep labels, coordinate meanings, source standing and uncertainty close to their existing representations | No invented trajectories, elevation scales or observations |
| Monochrome workstation and terrain references | Repeat a small set of controls and readable panes; keep provenance inspectable | No separate cosmetic dashboard or second shell |

Shared tokens, `Section`, `Inspector` and the existing HUD primitives give the console and specialist instruments a consistent visual language. These presentation components do not define acquisition, corpus identity, geometry or digest contracts.

**Verification for this iteration.** The full Vitest run passed 4,973 tests across 209 files; six optional GAT runtime tests remained skipped. After the final reduced-motion scrolling fix, the focused shell/HUD/primitives/palette run passed 109 tests, including the two new motion-preference cases. Typecheck, lint, production build and runtime trace checks passed. Loopback HTTP status checks returned 200 for `/`, `/product`, `/releases`, `/candidates`, `/notations`, `/earth` and `/production` (following redirects). The temporary preview/probe servers were stopped. No browser interaction, screenshot, responsive visual inspection or rendered accessibility audit was performed for this iteration; those checks remained pending explicit opt-in and have since been run — see the browser receipt immediately below. Earlier browser receipts further down are historical and must not be attributed to this theme.

## Verification receipt (2026-09-08, browser)

The checks the iteration above listed as pending have now been run, and this section is that receipt. The audit found one defect and closed it; the shell itself held everywhere it was measured.

**Responsive inspection and rendered accessibility.** `tests/e2e/breakpoints.spec.ts` runs the six widths this document names, on both sides of each transition — 1440 (desktop rail), 1024 and 1023 (rail becomes strip), 768 and 767 (header becomes three rows), 412 (phone) — across `/`, `/releases`, `/earth`, `/landshark` and `/production`. At each: no horizontal document overflow, no widened layout viewport, and the shell drawn to its stated contract — a 232 px column beside the surface with a vertical list above 1024, a horizontal strip above the surface below it, an 80 px minimum header of one row at 768 and above, a 9 rem minimum of three rows below. Then axe over `wcag2a`, `wcag2aa` and `wcag22aa` at every width, excluding only the WebGL canvas the globe engine owns, which is not this repository's markup and is excluded in the open rather than silently. 36 tests, all passing. The spec was itself checked against two token mutations (a 244 px rail, a 5 rem narrow header), which it failed on 20 of 36 — it measures the shell rather than describing it. This is a rendered audit and is separate from the static contrast arithmetic, which remains what it was.

**What the audit found.** One violation: `aria-prohibited-attr` on `/earth`, where the engine's licence-attribution container was a plain `div` carrying an `aria-label`, which names nothing an assistive technology may read. It is a named region now. Nothing else — no overflow and no layout-viewport widening at any of the six widths, on any of the five pages.

**The keys are named for the keyboard in front of the reader** (`platformKeys.ts`). Every hint used to read `⌘K` and `Alt`, on every machine, so a reader on Linux or Windows was told to press a key their keyboard does not have and a reader on a Mac was told Alt rather than Option. The behaviour was always right — the palette listens for `metaKey || ctrlKey`, and `altKey` is what Option sets — and only the naming was wrong. The server, which cannot know, renders both (`⌘/Ctrl K`), which is the accurate statement rather than a hedge; a browser that has said narrows it, and one that will not say keeps the pair. The `kbd` holds the widest form's width, so narrowing shifts nothing. The screen-reader text names both keys on every machine and detects nothing. Checked in the browser on this runner, which is one of the machines that had been told the wrong key, and against a mutation that answers APPLE for every platform.

**One defect found alongside it, in a shared primitive.** `FixtureBanner` could push a 412 px document 9 px wide and rescale the page. `min-w-0` let the flex item shrink; it did not let a 38-character documentation path inside it break, and that note measured 249 px in a 229 px box. `break-words` fixes it for every screen that carries the banner. The overflow guard had been catching this roughly one run in two and reporting a coin toss as a check; it and the breakpoint sweep now wait on `document.fonts.ready` before measuring, because a measurement taken mid-layout is not a measurement.

**The rest.** Typecheck, lint, the full Vitest run (223 files, 5,238 passing, 6 skipped — the GAT runtime tests, whose engine pin is `win32`), production build, and the full Playwright run at desktop and Pixel 7 (254 passing). Screenshots regenerated. New derivations were mutation-tested before being trusted: five mutations of `atlas.ts` and seven of `WorkspaceAtlas.tsx`, each caught — one of the seven escaped first time and closed a real gap, a vertical move that always landed on a band's first cell.

## The local rails were refusing every request (2026-09-08)

Verifying the notation workspace's new link against the real Rust kernel found something larger than the link. Every browser test in that suite failed at the first fetch, and so would every operator's: `requireLocalRequest` refused a request carrying any of eight forwarding headers, and **Next writes four of them onto every request it hands a route handler**, synthesised from the socket when the client did not send them. Measured on this server rather than supposed: a bare `curl http://127.0.0.1:3115/api/state-kernel`, with no proxy anywhere near it, arrives carrying `x-forwarded-for`, `x-forwarded-host`, `x-forwarded-port` and `x-forwarded-proto`. The notation kernel, the coordination board and the production inspector each refused their own browser.

The reasoning was right about proxies and wrong about this server, so the values are checked rather than the presence: a direct arrival's forwarded host is this host, its port is this port, its scheme is http and its peer is a loopback address. A reverse proxy's are none of those — the public host, usually https, the real client's address — so the deployment the guard exists to refuse is still refused, header by header, each named in the refusal. `forwarded`, `via`, `x-real-ip` and `x-cluster-client-ip` are still absolute: this server never writes one.

What this does not establish is unchanged and still stated: a caller who writes consistent loopback values into all four is not distinguished from a direct arrival, because nothing a handler can read proves how a request reached the socket. What keeps these rails local is the operator's — off by default, bound to loopback. The rails' own wrappers also stopped flattening the guard's finding into a bare `LOCAL_ONLY`; the code keeps its contract and the message now carries what was actually found, because a 403 saying only "same loopback origin" when the answer was "a proxy header named somewhere else" sends an operator to look in the wrong place.

**Verified.** The twelve real-kernel browser tests pass where all twelve failed; the four real-worker production tests pass. Eleven new guard cases: the headers this server writes admitted, each of a reverse proxy's four refused on its own, an appended forwarding list refused whether loopback leads or trails it, the loopback addresses a socket actually reports admitted, the forwarded host read against *this* host rather than a constant, and the four headers Next never writes refused whatever they say.

## The interaction pattern

Every working surface is `.workspace`: a centre surface in two parts (`.workspace-top`, `.workspace-bottom`) and, when an object is selected, a contextual `.inspector` (`src/components/primitives/Inspector.tsx`). From 1024 px the inspector is a sticky column beside the surface, spanning both parts; below that it is an inline detail view directly beneath the top part, so whatever the top part is (a register, a table) stays in reach above it and the forms beneath it stay reachable.

**Why the register's own width and not the viewport.** A viewport breakpoint could not express "the columns fit". The case queue's content column is 1015 px at 1023, where the navigation is a strip — and 758 px at 1024, where it is a column and the viewport is at its smallest. Wider viewport, narrower register. Every threshold picked against the viewport left a band where the table did not fit and a band where a list was drawn for no reason. Two container thresholds, both measured: the release and rulings registers fit in 750 px, the queue's columns do not fit under about 850. Verified at thirteen widths from 360 to 1600: no column hidden and no document overflow, on any of the three.

A surface whose centre is a wide register adds `.workspace-register` and keeps that inline view until 1440 px, which is measured rather than chosen: beside the inspector the register gets the viewport less the 232 px rail, the 400 px inspector and the page's padding — 760 px at 1440, which is exactly the release register, and 600 px at 1280, where a sixth of the table sits past its own right edge. A list of titles survives the narrower column and keeps the earlier breakpoint; a register does not. `.workspace-register-wide` is the same rule at the next size up, for a register that measures more: the case queue's three columns are 790 px, of which a status pill reading `ADMITTED WITH CONDITIONS` is 215 px and a status is a unit that does not break, so it sits beside the panel from 1600 px and inline below that. Nothing is modal. The inspector's heading takes focus on narrow screens when the object that opened it may be far away (`focusOnNarrow`), Escape closes it outside text fields, and closing returns focus to what opened it.

Selection connects views: a row pressed in a list, a button pressed on a card, a link followed from inside the inspector all set the same selection; the list marks the selected row and the inspector shows the object in context, with the actions allowed on it and a way to what it refers to.

**In a register, the row's own identity selects it; a reference to a different object is a link to that object.** That is the rule the three registers follow and it settles what used to be a coincidence: the release id and the ruling id select their rows, the case title selects its row in the queue, and the *case* named on a ruling's row is a link, because it is not that row. It also settles the one cost the queue paid for the pattern — opening a case is a press then the panel's primary **Open the case**, rather than one press on the title. That is the rule working, not a regression around it: a queue's job is deciding *whether* to open, the panel answers it in place, and a second affordance on the row's own identity would say that a name means two things.

*The corollary, in the same spirit as the register's own width:* what a control does is decided by what it names, not by where it sits.

**The coordination surfaces are registers too** (`StableRegister.tsx`, `BoardRegister.tsx`). They were the last card grids. Measured before the change: the stable put twelve participants in 415px cards, 2,969px of document at 1440×900 with two of twelve fully on the first screen, and 6,497px on a Pixel 7 with none — a stable is a thing you compare, and a reader holding two of twelve in view is remembering rather than comparing. Afterwards, 1,223px and seven of twelve. The board is the same shape for a different reason: three seeded messages already took 1.2 screens because every card carried its whole body, its receipts and its controls whether or not anyone was reading it, and a board is a record that accumulates. Both registers follow the rule above — a connection and a reply-to name another row, so they select it and the inspector moves; a release named on a message is a link, because a release is not one of these rows. The thread links used to be `#message-…` anchors that scrolled the page to a card; following one now moves the panel and leaves the register where the reader left it. Their preamble was two blocks saying the same thing twice, which cost a phone 124px before the first row; it is one statement now, with the claim that carries weight — *no agents are launched by this board* — first.

**The two specification surfaces** (`/corpora`, `/coverage`) put a plan on the register pattern rather than in a document nobody opens. Both read their domain module directly, so the page and the plan cannot drift, and both lead with what they are: no source named on `/corpora` is integrated, connected, licensed or collected, and no region on `/coverage` is entered. The counts that say so are derived — an integration is a candidate whose four gates have all passed, a corridor is a row in an empty table — and tests pass synthetic passed candidates and synthetic corridors to require the numbers to move, so the zeros mean something in both directions. **`ChainFigure`** is the new primitive both use: a run of distinct objects, numbered, each in its own frame, with an `aria-hidden` arrow between them. Position is sequence and nothing else is drawn, because nothing else is known — the same discipline the atlas follows. It is the right figure here because both specifications turn on the same move: a sequence of things that look like one thing and are not. A registered office and a factory. A notification and an enacted requirement. A point on the map and a verified facility.

**A stacked row is a grid, not a column of blocks.** Turning a register into a list solved the hidden column and left a second problem in place: six labelled cells one under another is the card the stacking replaced. Measured at 412px — the stable's row was 311px, releases 297, the board 323, rulings 280, and on four of the five surfaces not one row fitted the first screen. The short cells are short: a kind, a status, a runtime, a count. They sit two to a line now (`repeat(auto-fit, minmax(150px, 1fr))`), and only the row's own identity and the cells carrying a sentence — the queue's *required action*, a ruling's case, a message's correspondents, each marked `cell-wide` — take the full width. Measured after: 213, 199, 231, 237, with a row on the first screen where there had been none. The case queue is unchanged at 185 and should be: of its three columns one is the identity and one is a sentence, so there is nothing to pair. Narrower columns were tried and measured too — at `minmax(110px)` three fit across, which helped the stable and releases and made rulings and the board *worse*, because their cells then wrapped. Held by a test that asks the property rather than the pixel: in a stacked register, cells that are neither the identity nor a sentence occupy fewer lines than there are of them.

**A selected row is the one being read, so it is readable.** Every register marks its selection with the accent-tinted highlight, and everything muted inside that row — the identifier under a name, an empty cell's `None`, a status pill — was still the muted token: measured with axe, `#8e8b83` on the composited `#323223` is 3.81:1, where a 12.5px line needs 4.5. The fix raises `--text-muted` to `--text-secondary` *within* the selected row, so every muted thing in it is fixed at once, inline styles included, because `var()` resolves against the element it sits on. This was true of the case queue and the rulings register from the day they became registers, and nothing caught it, because no accessibility check had ever pressed a row. One now does, on all five registers, at a desk and on a phone — and it found a second thing in the same pass: the inspectors' *where to read it* lists distinguished their links from the sentence around them by colour alone, which is 1.4.1. They are underlined.

**A selection is a link** (`src/lib/hashSelection.ts`, `useLinkedSelection.ts`). An inspector that opens on a row and cannot be sent to anyone is half an inspector: the reader can see the thing and cannot say which thing they saw. Spatial Inquiry settled the shape — `#space=S-3`, read on load, written on every choice — and it is one codec now, so a reader learns the encoding once and the surfaces do not each invent one. The hash rather than the query, because writing a hash re-runs no server component: moving down a register costs nothing, and the server render never depends on which row a particular reader had open. Replaced rather than pushed, because six selections would otherwise be six entries in the back button and Back would stop meaning the page before this one. Both directions refuse a name the surface does not hold — an inspector opened on an id the bundle has never seen is a worse answer than the overview — and nothing is written until the surface's data has arrived, so a link is never erased by the page it was opened on still loading. On the case workspace the kind is the key (`#claim=`, `#evidence=`, `#invariant=`, `#ruling=`), so exactly one thing is selected by construction; on the notation workspace `#notation=` and `#relation=` are read together, and a link beats the selection this tab's drafts remembered, because it is the more explicit instruction.

## Slice one: notations (`/notations`)

**Getting between the thirty destinations.** The rail is one tab stop rather than thirty: a roving tabindex leaves exactly one link tabbable — the page you are on — so Tab enters the rail and Tab leaves it, and the arrow keys do the moving on both axes, because the same list is a column at 1024 px and a strip below it. `Alt`+←/→ steps through the rail order from anywhere; `⌘K` (or the **Jump to…** button, which is the only route on a touch screen) searches the same list and no other. The current tab is scrolled into the rail's own frame on arrival and after every navigation — measured before the change, a 412 px viewport put it 2113 px off-screen in a 2336 px strip that never scrolled. Measured after: 6 tab stops before the page content, down from 32. Full account in [UX architecture](UX_ARCHITECTURE.md) under "Getting between them".

Documented in [Notation workspace](NOTATION_WORKSPACE.md) under "Layout, selection and keyboard". In short: the register with each notation's identity, relation count and standing; the selected notation's inspector with its editor, its origin (created or changed in this draft and not saved, or in saved version N), its relations with direction, the pending commands that name it, and its evidence line; a relation's own inspector; keyboard undo, redo and save through the kernel; named progress. The draft lives in a root provider for the life of the browser document and in the tab's `sessionStorage` for a reload; the rail marks the Notations link `draft` while unsaved work exists, on every page, so no navigation is interrupted and nothing is lost.

## Slice two: candidate production (`/candidates`)

The rail is shown as a process, read only from what it recorded:

- **Four stages**, collection, extraction, normalization, candidate readiness (`processStages` in `src/domain/production.ts`). Every metric carries the field of the demonstration it is read from (`data-source`, shown on hover) and every instant its clock (capture time as declared, record time, decision time, run time, knowledge time, knowledge cutoff, build time). Extraction and normalization are one recorded run on this rail; the stage says so instead of inventing a step.
- **Where coverage stops** (`coverageGaps`): INGEST-only registrations, quarantines and refused steps, each with what stopped, the remediation someone could take, and the field it is read from. The rail takes none of the remediations; the screen performs none.
- **The inspector** on any acquisition, normalization run, build or refusal: provenance as a sequence (`acquisitionSequence`, `normalizationSequence`, `buildSequence`) with each step's identity, exact digest, time and clock, done or refused; for a candidate, evidence beside record: the captured bytes' identity and digest against the candidate's, then field by field the value in the source and the value in the record with what the adapter did between them (`fieldMapping`: copied, trimmed, or missing and not inferred). The source bytes themselves appear only when a committed file has exactly the evidence's content digest (`readCommittedSources` in the adapter, matched on the server); otherwise an explicit unavailable state names the storage key this screen does not open. Nothing on this rail is authored, and the inspector says so.

## Slice three: the Earth Twin (`/earth`)

Documented in [Earth Twin](EARTH_TWIN.md). The projection fabric's geodetic instrument on the same shell and inspector pattern: the globe is the surface, sticky beside a scrolling inspector from 1024 px and above it on narrow screens; the inspector says what the instrument is, what each layer is and where it comes from, what time it shows, what the corpus answered and where each declared position came from, which records are placed on the globe and which are not, which world signals are named and why none is on the globe, and what the current view's link is. Selection connects the views: choosing a record flies the camera to its declared position, and clicking a drawn point selects its record. It is keyless and offline by construction, and the browser test proves no request leaves the origin.

## Slice four: the production path (`/production`)

Documented in [Production path](PRODUCTION_PATH.md). The same shell and inspector pattern over the local production rail: the path rail across the top (seven stages, each a surface with its state pill, its detail, its exact object and its run), the run console beneath it (five steps in order, each with its request identity, its bound inputs, one primary action, and the rail's receipt rendered as returned), the inspector beside them for whatever output is reopened by exact reference, and beneath, the real source, the notation reference and the release with their blockers. Selection connects the views: an output button in any receipt opens the inspector; a member in a build opens its normalization; a candidate's provenance opens its acquisition. Recovery is where the failure is: the buttons under a quarantine or a refusal are the actions the rail's own retry policy permits, and a new identity renames the step and every later one in place. In fixture mode the console gives way to an empty state with the enable command, and the demonstration stands on the rail with links into the candidates page.

## Slice eight: the case queue (`/cases`)

The third of the four surfaces, and the one that measured worst of all: nine columns, of which **596 pixels sat past the right edge of a 1440 px viewport and 1012 past a 1024 px one** — more table hidden than shown, inside a scroll region a reviewer has no reason to suspect.

Three columns now, and they are the three a reviewer triages on: which case, where it stands, what it needs. Everything else is in the inspector, which is what makes the change worth its cost — because there is a cost, and it is worth naming. The case title used to be a link, so opening a case was one click and is now two: select, then **Open the case** from the panel. That is the right trade for a queue, because a queue's job is deciding *whether* to open, and the panel answers that in place — the alternative was loading five case pages to triage five cases. The register selects from its first cell, as the release and rulings registers do, so one interaction is learned once.

The instant of the last change moved to the panel, and the register keeps what a reviewer reads that column for: the rows are ordered newest-changed first, so recency *is* the order, and the `new` mark carries the part the order cannot say — that a case moved since you last looked. A column for the timestamp put 80 px of this register past its own right edge.

A filter hides rows, not cases. A link into a case the current filters exclude still opens on it, and the panel says the case is not in the filter rather than leaving the reader hunting a row that is not drawn.

**Verified.** `src/components/queue/CaseQueue.test.tsx` (6) and four mutations, each caught: the out-of-filter notice, the second press that closes, the opening link, and the panel's primary action. Browser tests that the three columns fit whole at the widths the queue is read at — beside the panel from 1600, inline beneath it below that — and that **Open the case** reaches the case workspace. Where the columns do not fit, at 1024 and on a phone, the register is a list instead and hides nothing.

## Slice seven: the rulings register (`/rulings`)

The second of the four surfaces this document listed as keeping their earlier layouts, and the one where the measurement was worst: nine columns, of which **531 pixels sat past the right edge of a 1024 px viewport** inside a silent scroll region. The declared use, the knowledge cutoff, the visibility class and the manifest commitment were all out there.

Five columns now: the ruling and its revision, its standing, the case it decided, when it was ruled, and on what assurance. Everything about one ruling is in the inspector — and the argument for putting it there is the revision chain. A ruling is never edited: a later revision supersedes it, the earlier one still says what it said, and the reason for the transition is recorded beside it. That is a chain with an annotation on every link, and a table cell can print one identifier of it. In the inspector each end is a button that selects that revision and the recorded reason is shown with it, so *"why is this no longer the ruling?"* is answered where it is asked — on the demonstration corpus, "Superseded by RUL-7C104-r2 after the sponsor attached the bill of lading and a custody record and resubmitted." A ruling with no reason recorded says that, rather than leaving the question unanswered.

The inspector also keeps the two clocks apart with the sentence that says why, counts the invariants by outcome and prints only the outcomes that occurred (a zero is not an outcome that happened), and carries the assurance class with the meaning recorded for it. A selection is a link (`#ruling=…`).

**A waiting surface names what it is reading.** Found while landing on this page: the application's only loading boundary said *"Loading case data from the current source…"* on every route — including the release catalogue, which reads the corpus, and the Earth Twin, which reads a release — in a 720 px centred column standing in for a 1400 px register, with no frame, no heading and no mark of the instrument. On a slow source that is indistinguishable from a surface that failed to render. `SurfaceLoading` replaces it, and the surfaces whose wait is worth naming carry their own.

**Verified.** `src/components/ruling/RulingRegister.test.tsx` (13) and six mutations, each caught: the outcomes filter, both directions of the chain, the recorded reason, an unheld link, the revision on the row, the world clock. `src/components/primitives/SurfaceLoading.test.tsx` (7) renders the boundary and reads every `loading.tsx` in the application, holding the root to naming no source it cannot know and each route to naming one it can — and refusing any claim to read case data from a route that does not. Browser tests at 1440 that no column is hidden inside the register's own scroll, that the chain walks with the row following the panel, and that the link opens on it cold.

*Not asserted in the browser, and stated rather than left as a gap:* the waiting boundary itself. It is transient by construction and the router prefetches a static route's flight response, so by click time there is nothing left to wait for; holding that response on hover and on click did not open it either, because the rail's own prefetch had already cached it. A test that passed sometimes would be worse than none, so it is covered by rendering the boundary and by the structural sweep instead.

## Slice six: the release register (`/releases`)

The first of the four surfaces this document listed as keeping their earlier layouts. The register carried nine columns: the coverage sentence, the build identity, the methodology, the digest and the supersession pointer sat in the same row as the counts. Two things followed, both measured on the rendered page rather than supposed. Every row was six lines tall, because one cell held a paragraph while the rest held a word. And the row would not fit — the supersession column sat past the right edge of a 1440 px viewport, inside a scroll region a reader has no reason to suspect, so the one field naming the release that replaced this one was the field nobody saw.

Six columns now, all of them visible at once: the release, its standing, its knowledge cutoff, what it holds, and whether it is certified — what a reader compares down a column. Everything about *one* release moved to the inspector beside it. The supersession chain is the argument for the change: a corrections history is a chain, a table cell can print one link of it, and in the inspector each end is a button that selects that release, so the chain is walked rather than read and looked up. Both ends say what they are when there is nothing there — the first release of the corpus, or one nothing has replaced — rather than showing an empty row. A selection is a link (`#release=…`), the arrow keys move through the register as they do on every other one, and a release the page does not hold selects nothing and is not left in the address bar claiming it does.

Nothing here computes: counts, certification, digests and the chain are the corpus's own fields, counted on the server and passed through.

**Verified.** `src/components/corpus/ReleaseRegister.test.tsx` (10: one row per release with no inspector until one is chosen, the paragraph fields absent from the table and present in the panel, the chain walked in both directions, both ends of the chain named when empty, close and re-press, arrow keys, a link opened and written, a link the page does not hold selecting nothing and clearing itself); five mutations, each caught, one of which escaped first time and closed a real gap — an unheld link left in the address bar while the page showed nothing. Browser tests at 1440 that no column is hidden inside the register's own scroll, that the chain walks and its link opens cold, and that the inspector sits beside the register only at the widths where the register still fits whole.

## Slice five: Spatial Inquiry (`/spatial`)

The linked plan/graph experience over the local spatial analysis service (contract: [Spatial Inquiry v1](spatial/inquiry-v1.md), `src/spatial/*`): how does opening or closing one explicitly declared passage change access through one floor? `src/domain/spatial.ts` reads the service's `payload.spatial-analysis-projection.v1` fail-closed (schema, `LOCAL_ANALYSIS`, the honest flags, a result shaped over the layout's own spaces) and turns it into drawing and reading: validated polygons flipped for the screen, passages between space centres, a graph in possible-depth columns with disconnected spaces last, a depth text in which null is never zero (`unknown` when only possibly reachable, `unreachable` when no graph reaches the space), a mean always shown with its denominator. The graph states its own information loss beside the figure, separately from the analysis's non-claims, because the drawing is a second representation of the same result: columns are depth and rows are an ordering by id, so neither carries distance; every passage at a space attaches at one point on its box; and two spaces at the same depth are joined by a curve bowed clear of the column, because drawn straight that passage would run backwards through the band both boxes occupy and under one of them. It computes no access: every depth, status, neighbour count and mean is the retained result's, and geometry is never used to make a passage.

`src/components/spatial/SpatialInquiry.tsx` is the surface on the shared shell: two saved request ids (defaults are the demonstration's baseline and closed-bridge scenario) inspected over `GET /api/spatial/analyses/{id}`, the comparison asked of `POST /api/spatial/compare` once both are in hand, a baseline/scenario view switch, the plan and the access graph as SVG whose spaces are buttons (`role="button"`, Enter and Space), a table with a button per row, and the inspector: the selected space's status, both depths, distinct neighbours, polygon; every passage naming it with declared and effective state, conditions and provenance, the scenario assumption marked beside the declared fact it never overwrites; the analysis' request, root, method and version, both means with denominators and the sentence that a smaller mean after a closure is not improved access, coverage and unresolved passage ids, scenario provenance; source references by acquisition and evidence digest (retained drawings are referenced, never embedded), layout, result and receipt digests, and the projection's standing flags verbatim. One selection connects plan, graph, table and inspector by space id, and a selection is a link (`#space=<id>`, read on load when it names a space of the layout, otherwise ignored). Without the local service the page says so with the exact remedy and fetches nothing; a service refusal is shown by its code, verbatim, with what it means; a projection outside the contract is refused and nothing is drawn in its place. Labels and provenance render as text, never markup.

**Verified.** `src/domain/spatial.test.ts` (6: contract reading and refusals, depth text, means with denominators and change text, plan geometry and Y flip, graph columns for baseline and closed-bridge, passages of a space and the selection link); `src/components/spatial/SpatialInquiry.test.tsx` (8: disabled service, baseline drawn from the retained example with the comparison asked once, synchronized selection by pointer and keyboard with the link, a linked selection restored and an unknown one ignored, the scenario view with declared facts kept, a refusal shown verbatim with the scenario view disabled and no comparison asked, a projection outside the contract and a disabled server refused, labels as text and other request ids inspected); `tests/e2e/spatial.spec.ts` under the isolated production runner (`npm run e2e:production`), which seeds the runner's evidence root with the retained demonstration and checks the page against the real service: plan, graph and table counts, P-09 unresolved, the means, selection across surfaces and by keyboard, the link, the scenario's closure of P-07, the comparison's three changed spaces, a fresh load of a selection link, no horizontal overflow at desktop and 412 px, no serious or critical axe findings, no page errors, no request leaving the origin. `/spatial` is in the overflow guard.

## What the design does not bend

- Fixtures are identified on the surface (the fixture banner, `FIXTURE` markers), never implied by polish.
- No control is shown for an operation the backend has no contract for: evidence-reference attachment stays disabled with the reason; no remediation is a button.
- A preview is never presented as saved, a candidate never as admitted, a source never as verified: origin lines, `UNADMITTED`, `UNRESOLVED`, `canonical null` and the not-claimed lists are rendered from the records.
- Evidence and interpretation stay apart (the notation evidence panel), evaluation and verification stay apart (source truth and field accuracy are not claimed), refused is not false (refusals are records of their own with the rail's words), valid time and knowledge time are labelled as such.

## Verification receipt (2026-09-05)

**Shell and design system.** Typecheck, lint, `next build`, 112 regular Playwright tests at desktop and Pixel 7 (keyboard smoke test through the primary navigation, overflow guard on every page, axe), screenshots regenerated. Commit `9969ed5`.

**Notation slice.** 158 unit tests in the notations and app trees (5 new), 112 regular browser tests, 10 real-kernel tests (one new per viewport), screenshots `00h-notations-inspector-desktop.png`, `00h-notations-inspector-mobile.png`, `00i-notations-relation-inspector.png` written by the real-kernel run. Commit `03fae40`. Receipt in [Notation workspace](NOTATION_WORKSPACE.md).

**Candidate production slice.** `src/domain/production.test.ts` (3 new: stage metrics and gaps against the committed demonstration, sequences with labelled clocks and refusals, field mapping bound to the adapter's own contract); `src/components/production/CandidatePipeline.test.tsx` (4: stages sourced and gaps before selection, evidence beside record, unavailable bytes and refusal follow-through with focus return, INGEST-only stop and build sequence); `tests/e2e/candidates.spec.ts` at desktop and Pixel 7 (metrics all sourced, five gaps, the candidate opened as evidence beside record with the committed bytes, the page's own counts unchanged by the inspector, layout beside the surface on desktop and brought into view with focus on mobile, refusal and quarantine and INGEST-only states, no horizontal overflow, no serious or critical axe findings, Escape). The existing candidates smoke test and the production separation test pass unchanged. Whole suites: 1063 unit tests, 114 regular browser tests, 10 real-kernel tests, screenshots including `00e2-candidates-inspector.png` and `11-candidates-inspector-mobile.png`.

**Draft-stabilization verification:** the provider retains drafts, capacity is reported by the API, and the rail displays the draft marker. Recorded validation: typecheck, lint, 1300 unit tests (the backend's twenty notation tests and this frontend's included; the pinned GAT runtime tests are excluded here because their engine pin is `win32` and this container is Linux, so they report the engine unavailable before any assertion), next build, 114 regular Playwright tests at desktop and Pixel 7, 10 real-kernel tests including edit → Releases → return with one kernel read and the marker, screenshots regenerated.

**Not done.** The inspector pattern is on two surfaces; cases, rulings, releases and coordination keep their earlier layouts inside the new shell. Source bytes are shown only for evidence whose bytes are committed in `examples/`; the local object store is not read by the screen. A selection is a link on Spatial Inquiry, the Earth Twin, the notation workspace, the case workspace, the case queue, the release register and the rulings register; the production path and the coordination surfaces still hold their selection in memory alone, so what is open there cannot be sent to anyone. The inspector pattern now reaches three of the four surfaces this document named; the coordination surfaces keep their earlier layouts inside the new shell. (Key hints did not name the platform's own modifiers; they do now — see the browser receipt above.)

## Slice five: the observation replay surface (`/compute/observations`)

Described in [Recorded observation replay](RECORDED_OBSERVATION_REPLAY.md). The same shell and inspector pattern over a contract rather than a rail: the frame diagram and the timeline are the centre's two pictures, both drawn from the manifest and the computation and nothing else, and both are selection surfaces; beneath them the observation and comparison registers; beside them the inspector for the selected observation. Distinctions are carried as badges from a closed vocabulary, so a synthetic input, a supplied estimate, an unresolved placement and a residual-only comparison never read as one another. Every state on the surface is the code the computation emitted, and every explanation is the meaning recorded for that code.
